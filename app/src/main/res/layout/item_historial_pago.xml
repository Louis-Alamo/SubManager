<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp">

    <!-- Ícono / círculo de color del servicio -->
    <ImageView
        android:id="@+id/ivAppIcon"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:scaleType="fitCenter"
        android:contentDescription="@null"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Nombre del servicio -->
    <TextView
        android:id="@+id/tvNombre"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="Netflix"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/ivAppIcon"
        app:layout_constraintEnd_toStartOf="@id/tvMonto"
        app:layout_constraintTop_toTopOf="@id/vColorDot"
        app:layout_constraintBottom_toTopOf="@id/tvFechaCategoria" />

    <!-- Fecha · Categoría -->
    <TextView
        android:id="@+id/tvFechaCategoria"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="15 Oct · Entretenimiento"
        android:textSize="11sp"
        android:textColor="@color/text_muted"
        app:layout_constraintStart_toEndOf="@id/ivAppIcon"
        app:layout_constraintEnd_toStartOf="@id/tvMonto"
        app:layout_constraintTop_toBottomOf="@id/tvNombre"
        app:layout_constraintBottom_toBottomOf="@id/vColorDot" />

    <!-- Monto negativo en rojo -->
    <TextView
        android:id="@+id/tvMonto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="-$199.00"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/error"
        android:layout_marginEnd="10dp"
        app:layout_constraintEnd_toStartOf="@id/tvBadgePagado"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Badge "Pagado" verde -->
    <TextView
        android:id="@+id/tvBadgePagado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/status_paid"
        android:textColor="@color/success"
        android:textSize="10sp"
        android:textStyle="bold"
        android:background="@drawable/bg_status_badge"
        android:backgroundTint="@color/success_bg"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
